version = '2.0'
group = 'com.acme'

apply plugin: 'java'
apply plugin: 'maven'
apply plugin: 'eclipse'


repositories {
  mavenCentral()
  mavenRepo ( url: "file://$buildDir/repo" )
}

dependencies {

  compile group: 'com.acme', name: 'server', version: '1.0'
}
